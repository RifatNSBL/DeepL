import numpy as np
import matplotlib as plt
from pattern import Checker, Circle, Spectrum
from generator import ImageGenerator
import os.path




if __name__ == "__main__":
    checker = Checker(20, 4)
    # circle = Circle(512, 20, (50, 50))
    # spectrum = Spectrum(123)
    # # checker.show()
    # circle.show()
    # # spectrum.show()

    # a = ImageGenerator("./src_to_implement/exercise_data/","./src_to_implement/Labels.json",
    #                    50, [32, 32, 3], rotation=False, mirroring=False, shuffle=True)
    # a.next()
    # a.next()
    # a.next()