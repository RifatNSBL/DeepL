import numpy as np
import matplotlib as plt
from pattern import Checker, Circle, Spectrum

if __name__ == "__main__":
    # Checker(20, 4)
    # Circle(100, 10,(20, 20))
    A = Spectrum(123)
    A.show()